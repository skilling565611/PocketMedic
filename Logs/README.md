# Logs

Runtime log files are written here by `Core/Logger.py`.

Log files are named by date: `YYYY-MM-DD.log`

This directory is tracked in version control (so it exists on fresh
clones) but its **contents** are ignored via `.gitignore`.
