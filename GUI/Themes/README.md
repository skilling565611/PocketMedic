# GUI Themes

Place theme files here. Each theme is a Python module that defines a
`COLORS` dict and a `apply_theme(root)` function for the Tkinter dashboard.

Example structure:

```
Themes/
├── default.py
├── dark.py
└── light.py
```
